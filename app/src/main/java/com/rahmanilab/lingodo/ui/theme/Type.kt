package com.rahmanilab.lingodo.ui.theme

import androidx.compose.material3.Typography

/**
 * Uses Material 3's default type scale. The system font renders both Latin and Persian text, so no
 * custom font is bundled; card content in Persian is laid out right-to-left where appropriate by
 * the individual composables.
 */
val LexicoTypography = Typography()
